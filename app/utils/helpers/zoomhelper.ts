export function useZoomHelper(
  gl: WebGL2RenderingContext,
  program: WebGLProgram,
  renderFunction:() => void,
  options: Record<any, any> = {}
) {

  // this function will have and host the mouse event for camera movement. 

}